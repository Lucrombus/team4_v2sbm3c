<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace = "dev.mvc.rankurl.RankurlDAOInter">
  
  <insert id="create" parameterType="dev.mvc.rankurl.RankurlDAOInter">
    INSERT INTO rankurl(rankurlno, rankno, contactadno) 
    VALUES (rankurl_seq.nextval, #{rankno}, #{contactadno});
  </insert>
  
  <select id="list" resultType="dev.mvc.rankurl.RankurlDAOInter">
    SELECT rankurlno, rankno, contactadno
    FROM rankurl
    ORDER BY rankurlno ASC;
  </select>
  
  <!-- rankurl로 등급 및 접속 주소 번호 조회 -->
  <select id="read" resultType="dev.mvc.rankurl.RankurlDAOInter" parameterType="int">
    SELECT rankurlno, rankno, contactadno
    FROM rankurl
    WHERE rankurlno = #{rankurlno}
  </select>  
  
  <!-- rankurlno로 등급 변경 -->
  <update id="update" parameterType="dev.mvc.rankurl.RankurlDAOInter">
    UPDATE rankurl 
    SET rankno=#{rankno}
    WHERE rankurlno = #{rankurlno}
  </update>
  
  <delete id="delete" parameterType="int">
    DELETE FROM rankurl
    WHERE rankurlno=#{rankurlno}
  </delete>

</mapper>