<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace = "dev.mvc.inquiry.InquiryDAOInter">
  
  <insert id="create" parameterType="dev.mvc.inquiry.InquiryDAOInter">
   INSERT INTO inquiry(inquiryno, memberno, inquiryTitle, inquiryReason, answer) 
   VALUES (inquiry_seq.nextval, #{memberno}, #{inquiryTitle} , #{inquiryReason}, #{answer}) 
  </insert>
  
  <!-- 전체 문의 목록 출력 (관리자)-->
  <select id="list_all" resultType="dev.mvc.inquiry.InquiryVO">
    SELECT inquiryno, memberno, inquiryTitle, inquiryReason, answer
    FROM inquiry
    ORDER BY inquiryno ASC
  </select>
  
  <!-- 회원 본인 문의 목록 출력 -->
  <select id="list_by_memberno" resultType="dev.mvc.inquiry.InquiryVO">
    SELECT inquiryno, memberno, inquiryTitle, inquiryReason, answer
    FROM inquiry
    WHERE memberno = #{memberno}
    ORDER BY inquiryno ASC
  </select>

  <!-- memberno와 inquiryno에 따른 문의 정보 조회 -->
  <select id="read" resultType="dev.mvc.inquiry.InquiryVO" parameterType="int">
    SELECT inquiryno, memberno, inquiryTitle, inquiryReason, answer
    FROM inquiry
    WHERE inquiryno = #{inquiryno}
  </select>  
  
</mapper>