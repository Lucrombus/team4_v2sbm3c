<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.like_reply.Like_replyDAOInter">

 <!-- 등록: 추가한 레코드 갯수를 리턴 -->
  <insert id="create" parameterType="dev.mvc.like_reply.Like_replyVO">
  INSERT INTO reply (replyno, contentsno, memberno, reply_content, rdate)
  VALUES (reply_seq.nextval, #{contentsno}, #{memberno}, #{reply_content}, sysdate)
  </insert>
  
     <!-- 조회: 댓글 번호별 댓글 조회 -->
  <select id="read" resultType="dev.mvc.like_reply.Like_replyVO" parameterType="int">
  SELECT replyno, memberno, contentsno, reply_content, rdate
  FROM reply
  WHERE replyno = #{replyno}
  </select>
  



</mapper>